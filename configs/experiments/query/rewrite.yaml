extends: ../hybrid_reranker.yaml
experiment:
  id: query-rewrite-hybrid-reranker-v1
retrieval:
  query_processor:
    provider: rewrite
    model: nvidia/nemotron-3.5-content-safety:free
    max_queries: 1
