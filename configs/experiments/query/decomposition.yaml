extends: ../hybrid_reranker.yaml
experiment:
  id: query-decomposition-hybrid-reranker-v1
retrieval:
  query_processor:
    provider: decomposition
    model: nvidia/nemotron-3.5-content-safety:free
    max_queries: 4
